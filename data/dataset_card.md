# 帮信罪数据集 v1.0

> 本文档由 `backend.scripts.split_dataset` 自动生成。

## 1. 数据集基本信息

- 版本: `v1.0`
- 生成时间: 2026-06-12T04:53:40.419421+00:00
- 划分种子 (seed): `42`
- 划分比例 (train:val:test): `0.7:0.2:0.1`
- 划分策略: `stratified_by_final_verdict`
- 数据来源: `cases` 表 + `case_labels` 表 (SQLite)
- 完整标注案件总数: **100**

## 2. 各集合大小

| 集合 | 案件数 | 比例 | 用途 |
|------|------:|----:|------|
| train | 70 | 70.0% | 模型训练与 prompt 调优 |
| val | 20 | 20.0% | 超参选择 / 早期停止 |
| test | 10 | 10.0% | **不参与训练**，仅用于最终评估 |

**test 集合为硬编码锁定集**：保存为 `data/test_set_v1.0.jsonl`，后续 prompt 调优 / 训练 / 超参搜索不得使用 test 集合中的数据。

## 3. 标签分布

### 3.1 全量分布

**`d1_tier`**

| 取值 | 数量 |
|------|----:|
| 三档 | 32 |
| 四档 | 30 |
| 一档 | 22 |
| 二档 | 16 |

**`final_verdict`**

| 取值 | 数量 |
|------|----:|
| 认定帮信 | 54 |
| 不认定帮信 | 30 |
| 竞合 | 16 |

**`verdict_subtype`**

| 取值 | 数量 |
|------|----:|
| 供述明知 | 59 |
| 获利明显 | 14 |
| 其他 | 10 |
| 客观推定 | 9 |
| 被骗开卡 | 4 |
| 仅有流水 | 4 |

**`judicial_era`**

| 取值 | 数量 |
|------|----:|
| 2025意见前 | 100 |

### 3.2 各集合分布

#### train

- **`d1_tier`**: {'三档': 22, '四档': 21, '一档': 16, '二档': 11}
- **`final_verdict`**: {'认定帮信': 38, '不认定帮信': 21, '竞合': 11}
- **`verdict_subtype`**: {'供述明知': 42, '获利明显': 9, '其他': 8, '客观推定': 5, '仅有流水': 3, '被骗开卡': 3}
- **`judicial_era`**: {'2025意见前': 70}

#### val

- **`d1_tier`**: {'三档': 6, '四档': 6, '一档': 4, '二档': 4}
- **`final_verdict`**: {'认定帮信': 11, '不认定帮信': 6, '竞合': 3}
- **`verdict_subtype`**: {'供述明知': 12, '获利明显': 3, '客观推定': 2, '被骗开卡': 1, '其他': 1, '仅有流水': 1}
- **`judicial_era`**: {'2025意见前': 20}

#### test

- **`d1_tier`**: {'三档': 4, '四档': 3, '一档': 2, '二档': 1}
- **`final_verdict`**: {'认定帮信': 5, '不认定帮信': 3, '竞合': 2}
- **`verdict_subtype`**: {'供述明知': 5, '获利明显': 2, '客观推定': 2, '其他': 1}
- **`judicial_era`**: {'2025意见前': 10}

## 4. 数据格式

每行一条 JSON 记录，字段说明：

| 字段 | 类型 | 说明 |
|------|------|------|
| `case_id` | string | 原始案件 ID (例如 `CASE_0000`) |
| `db_pk` | int | 数据库主键 |
| `title` | string | 案件展示标题 |
| `case_number` | string \| null | 案号 (例如 `(2024)黔刑初001号`) |
| `description` | string \| null | 案件描述 |
| `status` | string | 案件状态 |
| `case_text_preview` | string | 案件事实文本前 500 字符预览 |
| `d1_tier` | string | 维度分档 (一档/二档/三档/四档) |
| `final_verdict` | string | 最终定性 (认定帮信/不认定帮信/竞合/无罪/其他) |
| `verdict_subtype` | string | 认定子类 (主动核实/仅有流水/被骗开卡/熟人借用/获利明显/供述明知/客观推定/其他) |
| `judicial_era` | string | 司法时期 (2019解释/2025意见前/2025意见后) |

## 5. 使用限制与注意事项

1. **test 集合禁止用于训练**：任何 prompt 调优、模型微调、超参搜索都必须排除 test 集合。
2. **数据来源**：当前数据集来自 `data/raw/` 目录下 100 份原始 JSON 文件，经去重与人工/CLI 标注后形成。
3. **隐私**：原始文本已脱敏，但应避免对外发布原始 `case_text` 字段。
4. **版本管理**：后续 `v1.1` / `v2.0` 重新划分时，请保留 test 集合的 case_id 列表以保证可对比。
5. **小样本警告**：本数据集样本量较小（30~100 条），仅适合做方法验证与 prompt 工程评估，不适合用于严肃的统计推断。

## 6. 输出文件

- `data\test_set_v1.0.jsonl` (test)
- `data\training\train_set_v1.0.jsonl` (train)
- `data\training\val_set_v1.0.jsonl` (val)
